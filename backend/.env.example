# Copy to .env and fill with your production values
PORT=5000
MONGODB_URI=mongodb://localhost:27017/portfolio
JWT_SECRET=change_this_in_production
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD=changeme
# Comma-separated origins are allowed.
CORS_ORIGIN=http://localhost:4200,https://imeshbandara.netlify.app
# Optional alternative if you prefer a single dedicated frontend env var.
FRONTEND_ORIGIN=
